{
  "name": "@cakyu-helper/extension",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "generate:mapping": "bun run scripts/generate-default-mapping.ts",
    "firefox": "bun run build && web-ext run --source-dir dist",
    "lint:firefox": "web-ext lint --source-dir dist --self-hosted"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "catalog:",
    "web-ext": "^10.7.0"
  }
}
