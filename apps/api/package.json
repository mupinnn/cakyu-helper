{
  "name": "@cakyu-helper/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "bun run --hot ./index.ts",
    "build": "bun build --compile --minify ./index.ts --outfile cakyu-helper-api"
  },
  "dependencies": {
    "@cakyu-helper/shared": "workspace:*",
    "@googleapis/calendar": "^12.0.0",
    "@hono/zod-validator": "^0.7.5",
    "hono": "catalog:",
    "puppeteer-core": "^24.22.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "catalog:"
  }
}
